[tool.maturin]
python-source = "python"
module-name = "poke_engine.poke_engine"

[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "poke-engine"
version = "0.0.45"
readme = "readme.md"
